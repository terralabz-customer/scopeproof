# ScopeProof demonstration plan

**Format: 136-second captioned montage of screenshots captured from actual local runs.** This is an edited screenshot presentation, not a continuous or real-time screen recording. Processing gaps are omitted. It uses synthetic data and has no audio track. The encoded video and public upload remain pending verification.

The source captions and timings are in `demo-scenes.json`. They describe observed runs on 13 September 2026 using Strands Agents, local Ollama and qwen3:4b-instruct. Code and documentation were created with AI coding assistance.

## Seven scenes

1. **0:00–0:12 — Problem and audience.** Show the overview. Explain that ScopeProof helps freelance developers turn client messages into fixed-scope work. The opening caption discloses the montage format and omitted processing gaps.
2. **0:12–0:28 — Synthetic brief.** Show the request for one responsive landing page, four sections and supplied copy/images. Label the brief, four catalog services and all prices as illustrative.
3. **0:28–0:48 — Actual sample estimate.** Show the real USD 120 result. The caption reports 33.02 seconds of CPU processing and two tool calls; the video segment length is not the inference duration. Explain that code applies the catalog price.
4. **0:48–1:08 — Source evidence.** Show the preserved source sentences. Explain that code checks capability IDs and quantities, while exact text does not prove feasibility or correct interpretation.
5. **1:08–1:32 — Added work and withheld price.** Show the second actual run adding login and subscription checkout. It took 30.30 seconds. The static-page estimate becomes unpriced and the added work is excluded.
6. **1:32–1:54 — Review pack.** Show the draft reply and export interface. The actual downloaded ZIP contained the scope review, draft message, evidence/audit JSON and proposed delivery checklist. Its price and evidence were checked against the result. No message was sent and no client development work was performed.
7. **1:54–2:16 — Trace and limits.** Show the separate real CSV run's details: 31.28 seconds, two model calls and two tool calls. Explain the local Strands/Ollama/FastAPI stack and the continuing need for human review.

## Final review before upload

- Verify the rendered duration is 136 seconds and all captions are legible.
- Confirm every scene uses the intended actual-run screenshot and no private data is visible.
- Preserve the opening disclosure that this is an edited screenshot montage with no audio.
- Check that captions distinguish the observed processing times from the scene durations.
- Do not describe the synthetic catalog prices as money earned, accepted quotes or prizes.
- Publish the actual video to YouTube or Vimeo with public visibility, then verify its public URL signed out.
- Put the verified URL in the Devpost draft; final submission and receipt remain separate steps.

The montage documents observed prototype behavior. It does not demonstrate general semantic accuracy, customer adoption or an AWS deployment.
